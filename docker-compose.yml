services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: personal-website
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
